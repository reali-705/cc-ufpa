// A cada novo diretorio (activity) sera necessario um novo tsconfig.json
{
    "files": [],
    "references": [ // diretorios para automatizar a compilacao/transpilacao
        { "path": "./activity 3" },
        // add mais path's conforme necessario
    ]
}
// comando para transpilar todos os arquivos: tsc -b