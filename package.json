{
  "name": "no-man-sky",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "b": "tsc && find Javascript -type f -name '*.js.map' -exec mv {} Javascript/mappings \\; && find Javascript -mindepth 1 -type d -empty -delete",
    "b-w": "tsc && ( if not exist Javascript\\mappings\\ mkdir Javascript\\mappings ) && for /r Javascript %f in (*.js.map) do move \"%f\" Javascript\\mappings\\",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ale-Reali-705",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.0.7"
  }
}
